# Dataset (not committed)

Place your **COCO-format** export here (for example from [Roboflow](https://universe.roboflow.com/carolina-rutili-de-lima/many_toget)).

Typical layout used by the notebooks:

- `aug_1/train/` — images + `train.json` (or your annotation filename)
- `aug_1/valid/` — images + `valid.json`

Point the paths in each notebook’s constants to match your folders.
